# N0ELZIN Motion Bio

Open `index.html` in a browser.

The site already uses the provided Spotify artist image and includes active links for Spotify, SoundCloud, Instagram, and GitHub.

Add either `audio.mp3` or `audio.wav` to this folder. It starts automatically after the visitor clicks **ENTER EXPERIENCE**.
