const root=document.documentElement, portrait=document.querySelector('#portrait'), sound=document.querySelector('#sound'), siteAudio=document.querySelector('#siteAudio');
window.addEventListener('pointermove',e=>{root.style.setProperty('--x',e.clientX+'px');root.style.setProperty('--y',e.clientY+'px');if(portrait){const r=portrait.getBoundingClientRect();const x=(e.clientX-r.left-r.width/2)/r.width;const y=(e.clientY-r.top-r.height/2)/r.height;portrait.style.setProperty('--moveX',`${x*24}px`);portrait.style.setProperty('--moveY',`${y*18}px`)}});
document.querySelectorAll('.link-card').forEach((card,i)=>card.addEventListener('click',e=>{if(card.getAttribute('href')==='#'){e.preventDefault();card.querySelector('strong').animate([{letterSpacing:'-.07em'},{letterSpacing:'.03em'},{letterSpacing:'-.07em'}],{duration:420})}}));
sound.addEventListener('click',()=>{siteAudio.muted=!siteAudio.muted;if(!siteAudio.muted)siteAudio.play().catch(()=>{});sound.innerHTML=`<span data-i18n="${siteAudio.muted?'soundOff':'soundOn'}">${siteAudio.muted?'SOUND OFF':'SOUND ON'}</span> <i></i>`;window.applyLanguage?.()});
document.querySelector('#top').addEventListener('click',()=>window.scrollTo({top:0,behavior:'smooth'}));
